type User {
    id: ID!
    firstName: String!
    lastName: String!
    email: String!
}

type Query {
    user: User
    users: [User!]!
}

type CreateUserInput {
    firstName: String!
    lastName: String!
    email: String!
}

type Mutation {
    createUser(input: CreateUserInput!): User
}
